<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Verstka_last" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/black</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="mainActivityBackgroundColor">@color/blue</item>
        <item name="settingsTextColor">@color/black</item>
        <item name="settingsIconBackColor">@color/black</item>
        <item name="settingsBackgroundColor">@color/white</item>
        <item name="colorAccent">@color/blue</item>
        <item name="settingsIconTint">@color/gray</item>
        <item name="searchIconTint">@color/black</item>
        <item name="searchButtonUpdateTint">@color/black</item>
        <item name="searchButtonUpdateTextColor">@color/white</item>
        <item name="playButtonIcon">@drawable/ic_button_play</item>
        <item name="favoriteButtonIcon">@drawable/ic_button_add_favorits</item>
        <item name="playlistButtonIcon">@drawable/ic_button_add_to_playlist</item>
        <!-- Customize your theme here. -->
    </style>
</resources>